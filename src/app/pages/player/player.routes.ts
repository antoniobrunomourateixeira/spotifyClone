import { Routes } from '@angular/router';
import { PlayerComponent } from './player.component';

export const PlayerRouts: Routes = [
    {
        path: '',
        component: PlayerComponent
    }
] 